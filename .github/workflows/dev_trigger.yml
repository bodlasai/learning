name: Call a reusable workflow

on:
  workflow_dispatch:

jobs:
  builds:
    uses: bodlasai/github-actions/.github/workflows/testing_build.yml@master
    secrets:
      AWS_ACC_KEY: $${{ secrets.AWS_ACC_KEY }}
      AWS_SEC_KEY: ${{ secrets.AWS_SEC_KEY }}
